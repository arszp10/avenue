Надо:

- Работа с одним перекрестком
    (Рассчет длительности цикла, фаз, и т.д.)
- Исключение перекрестков из оптимизации(признак)
- Ошибки при расчете графа состоящего только из циклов
- Последовательность включения фаз
    (несколько последовательностей, выбор оптимальной)
---------------------------------------------

- Импорт SUMO (обработчик ошибок)
- Импорт Net329Editor
- Рассчет надежности ПК при случайном характере входных парамтеров
    (Монте-карло, ГПСЧ с нормальным распределенеим, на каждом шаге меняем (Интенсивности, потоки насыщения, время проезда)
     подсчет заторных точек, распределение вероятностей числа заторов, )
- Отрисовка треков модельных и реальных, загрузка треков общественного(любого) транспорта, гео-привязка точек
    (Оптимизация с учетом треков ОТ, например штраф за попадание трека на стоплинии в красную фазу, на перв взглад кажется, что надо приводить к количеству пассажиров
     и считать PI(задержку не ТС, а пассажиров при средней загрузке ТС) таким образом  каждый автобус будет иметь вес в ~50 раз больше что и будет давать штраф
     за попадание на красный -  мб получится пассивный приоритет.)
- Пользовательские настройки, смена пароля итд
- Логи сервера
- API запрос на рассчет/оптимизацию сохранненной модели с новыми числовыми параметрами
    (В запросе передается небольшое число измененных значений, для тех или иных точек)
- Мои расчеты, мои шаблоны
- SEO

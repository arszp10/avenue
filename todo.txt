Надо:
- Пешеходы
    - Редактирование точек (ед изм, поля)
    - Ограничения для апи запроса(валидация)
    - модель (обычная стоп линия если в составе перекрестка)
        время занятое пешеходами
        длинна перехода
        скорость пешехода
        человеческие задержки
        внести коррективы в PI

- Цикл моделирования, циклы регулирования на смежных перекрестках (убрать ограничение на равный цикл, так как это частный случай)

- Модельниые треки на диаграме время путь. Построение сложных маршрутов с учетом разных точек и задержек на точке конкурент

- Градиентный метод обучения(подбора) интенсивностей при заданных средних сокростях(временах проезда)

- Попробовать дополнительно сделать расчет длит цикла/фаз по вебстеру
  с учетом рекомендаций в методичке http://rodosnpp.ru/media/rodos/normative_base/nt_dejstv/odm/_218.6.003-2011.pdf
  пункт 11.16

- косяки при рендеринге зеленых стрелок в в форме перекрестка сложных случаях

- баг с привязкой к карте и открытии модели на другом размере окна

- мультиринг?

- Исключение перекрестков из оптимизации(признак и учет этого признака)

- А не сделать ли нам длинну и ширину пееркрестка в мтерах и расчет длины дуг(стрелок) в метрах на базе этих параметров
 + неявные точки перегон
 + на базе известных длин и скоросетй движения по перекрестку рассчет/контроль пром такта и длит фаз где ходят пешеходы

- Админский доступ к моделям

- сумма входящих пересчет при зацеплении

- Пользовательские настройки, смена пароля итд

- Импорт SUMO (обработчик ошибок)

- Импорт Net329Editor

- Рассчет надежности ПК при случайном характере входных парамтеров
    (Монте-карло, ГПСЧ с нормальным распределенеим, на каждом шаге меняем (Интенсивности, потоки насыщения, время проезда)
     подсчет заторных точек, распределение вероятностей числа заторов, определение лямбда если оно будет все-таки Пуассона )

- Отрисовка треков модельных и реальных, загрузка треков общественного(любого) транспорта, гео-привязка точек
    (Оптимизация с учетом треков ОТ, например штраф за попадание трека на стоплинии в красную фазу, на перв взглад кажется, что надо приводить к количеству пассажиров
     и считать PI(задержку не ТС, а пассажиров при средней загрузке ТС) таким образом  каждый автобус будет иметь вес в ~50 раз больше что и будет давать штраф
     за попадание на красный -  мб получится пассивный приоритет.)

- Логи сервера

- API запрос на рассчет/оптимизацию сохранненной модели с новыми числовыми параметрами
    (В запросе передается небольшое число измененных значений, для тех или иных точек)

